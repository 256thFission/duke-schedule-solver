[project]
name = "duke-schedule-solver"
version = "1.0.0"
description = "Optimal course schedule generator for Duke University"
license = {text = "MIT"}
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["scripts*", "backend*"]
